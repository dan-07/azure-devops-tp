from src.app import add, app

def test_add():
    assert add(2, 3) == 5

def test_index():
    client = app.test_client()
    resp = client.get('/')
    assert resp.status_code == 200
    assert b'Hello from Azure DevOps TP' in resp.data
